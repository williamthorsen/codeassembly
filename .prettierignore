pnpm-lock.yaml
**/.readyup/manifest.json

# Test fixtures no parser can read, marked by a `.malformed` infix. Prettier cannot parse
# them, and reformatting would erase the defect they exist to test.
**/__tests__/**/fixtures/**/*.malformed/
**/__tests__/**/fixtures/**/*.malformed.*
