- 2026-04-16: Wrote integration test that ran a full vitest suite as a side effect just to verify a log message prefix, then proposed increasing the timeout instead of questioning the design → Skill should state: Tests must do the minimum work needed to verify the behavior under test; use lightweight fixtures (e.g., temp directories with trivial scripts) instead of exercising real heavyweight operations. Treat timeout increases as a red flag that the test is doing unnecessary work, not as a solution.
