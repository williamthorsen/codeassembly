# User-global registry fixture used to verify the user config is read when no project registry exists.
# Paths are relative to this file's directory (`home-with-default/.agents/`).
default_kb: global-vault
kbs:
  global-vault:
    path: ../../vault-b
    description: Fixture global vault
  named-vault-a:
    path: ../../vault-a
    description: Fixture vault A registered by name
