# User-global registry fixture used to verify the user config is read when no project registry exists.
# Paths are relative to this file's directory (`home-with-default/.claude/`).
kbs:
  global-vault:
    path: ../../vault-b
    description: Fixture global vault
    default: true
