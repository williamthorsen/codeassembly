# Project-local registry fixture: one extra KB plus a default-marked global vault.
# Paths are relative to this file's directory (`project-kb/.agents/`).
kbs:
  vault-a:
    path: ../../vault-a
    description: Fixture vault A
    default: true
  vault-b:
    path: ../../vault-b
    description: Fixture vault B
