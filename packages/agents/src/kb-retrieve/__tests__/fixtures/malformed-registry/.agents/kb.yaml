# Malformed registry fixture: invalid YAML so `loadKbConfig` throws.
# `resolveScope` must degrade this to an empty registry rather than propagating the throw.
kbs:
  vault-a:
    path: ../../vault-a
   default: true
  : [unterminated
