/** Tile size in pixels, matching the prototype's 32px LimeZu tilesets. */
export const TILE_SIZE = 32;

/** Canvas width in tiles. */
export const CANVAS_COLS = 36;

/** Canvas height in tiles. */
export const CANVAS_ROWS = 22;

/** Canvas width in pixels. */
export const CANVAS_WIDTH_PX = CANVAS_COLS * TILE_SIZE;

/** Canvas height in pixels. */
export const CANVAS_HEIGHT_PX = CANVAS_ROWS * TILE_SIZE;

/** Stagger delay between transitions in milliseconds. */
export const TRANSITION_STAGGER_MS = 150;
