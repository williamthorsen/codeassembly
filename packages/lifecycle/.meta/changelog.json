[
  {
    "version": "0.4.2",
    "date": "2026-09-01",
    "sections": [
      {
        "title": "🧪 Tests",
        "audience": "dev",
        "items": [
          {
            "description": "Consolidate console silencing on toolbelt.vitest's silenceConsole (#1413)",
            "body": "Consolidates console silencing across the test suites on `silenceConsole` from `@williamthorsen/toolbelt.vitest/candidate`, replacing a local copy of that function and hand-rolled `vi.spyOn(console, …)` pairs. The conversion also closes a spy leak in agents' `captureList`, where a throw from the command under test left `console.info` and `console.warn` mocked for the rest of the file.",
            "hash": "4deeb1e57f5fa7bf4dbb0edb5c80c4f000838019"
          }
        ]
      },
      {
        "title": "📦 Dependencies",
        "audience": "dev",
        "items": [
          {
            "description": "Upgrade all dependencies and adopt nmr's Vitest factory defaults (#1427)",
            "body": "Upgrades eight dependencies, most notably `@williamthorsen/nmr` to 0.34.0. That upgrade moves source resolution and git isolation into nmr's Vitest factory, so the repo's hand-rolled `.config/vitest/` layer is deleted, and a package carries a Vitest config only where it configures something of its own.\n\nSeparately, `@types/node` is declared in every workspace package and named in the root `tsconfig.json`'s `types`, which TypeScript 6 requires for a package that imports a `node:` builtin.",
            "hash": "c1288ff93e755d9346c518e812e2b6e1b094068c"
          },
          {
            "description": "Declare the configuration files' dependencies and restore the lint rules that enforce them (#1428)",
            "body": "Declares `eslint`, `@williamthorsen/nmr`, and `vite` in every package whose configuration files import them, and pins each once in the workspace catalog. Restores two `eslint-plugin-n` import rules the root configuration had switched off, so that a configuration file importing a package not declared in its own manifest now fails the lint gate.",
            "hash": "8cf79d48201de0d38ef7ef9fb5a4049ee336c236"
          },
          {
            "description": "Consolidate shared dependency pins into the catalog and guard the rule (#1432)",
            "body": "Moves every dependency pinned by two or more manifests into `pnpm-workspace.yaml`'s `catalog:` block. A test enforces the use of the catalog whenever a dependency has more than one consumer or is already listed in the catalog.",
            "hash": "b3836586645dfbb7f9f69cc90c1eac30cc540eff"
          }
        ]
      }
    ]
  },
  {
    "version": "0.4.1",
    "date": "2026-08-20",
    "sections": [
      {
        "title": "⚙️ Tooling",
        "audience": "dev",
        "items": [
          {
            "description": "Activate import/extensions so a .js specifier naming a .ts file fails lint (#1311)",
            "body": "Adds `eslint-import-resolver-typescript` and wires it into the repo-root ESLint config, so `import/extensions` now reports a relative `.js` specifier that names a `.ts` file, dynamic `import()` included. `packages/factory` is currently exempt.\n\nSeparately, every package's `eslint.config.ts` now extends the repo-root config instead of importing `@williamthorsen/eslint-config-typescript` directly. That inheritance is what carries the resolver into a per-workspace lint run, and a guard fails when a package config bypasses the root.",
            "hash": "46afb2f7305fadda7a731eb7ef061943d9e8cfcc"
          }
        ]
      }
    ]
  },
  {
    "version": "0.4.0",
    "date": "2026-08-13",
    "sections": [
      {
        "title": "🎉 Features",
        "audience": "all",
        "items": [
          {
            "description": "Attach causes to kb's loader errors and retire its lint deferral (#1272)",
            "body": "Errors `kb` raises on malformed input now carry the underlying parse error as their cause. Callers constructing one of these errors themselves can attach a cause. When `kb` reports a thrown error that carries no message, the diagnostic now names the error's class instead of trailing off empty.",
            "hash": "dbc91bd62d8ea7ab2e3c1d9d4790ce55e4be810b"
          }
        ]
      },
      {
        "title": "♻️ Refactoring",
        "audience": "dev",
        "items": [
          {
            "description": "Fix lint and retire rule deferrals (#1265)",
            "body": "Fixes deferred lint violations in the `lifecycle`, `run-core`, and `foreman` packages and removes the cap on the severity of associated rules when a strict-lint check is run.",
            "hash": "70f17b8fc2ba06c8dfb1fde4bcc2128069e8b59f"
          }
        ]
      },
      {
        "title": "⚙️ Tooling",
        "audience": "dev",
        "items": [
          {
            "description": "Remove shelled nmr calls from package manifests (#1291)",
            "body": "`nmr build` no longer reaches nmr through a shell, so a failing package build reports the step that failed instead of the whole nested subtree, and interrupting a run stops what would have followed. Five packages had a `build` override doing that: `kb`, `lifecycle`, `mcp`, and `run-core` restated a default nmr already supplies and now resolve `build` to its built-in `['compile']`, and `codeassembly`'s two post-compile steps move into its `build:post` hook. The warning nmr printed for each, five on every build, goes with them.\n\nA root test keeps the pattern out: It fails when any manifest in the repo, the monorepo root's included, declares a script reaching `nmr`. It catches the forms nmr's own warning misses, `npx nmr` and `pnpm --recursive exec nmr` among them, and exempts the npm lifecycle names along with root's `bootstrap`.",
            "hash": "8cc3027bc0abf4966b66ba61900f8a1e933fc076"
          },
          {
            "description": "Upgrade eslint-config-typescript to 10 and complete manifest metadata (#1301)",
            "body": "Upgrades `@williamthorsen/eslint-config-typescript` to v10 and adds missing `package.json` values required by rules activated in the upgrade.\n\n`codeassembly`'s empty `exports` had been the declaration that the package has no library surface. This has been changed to `{\"./package.json\": \"./package.json\"}`, which complies with the new rules without exposing an importable module.",
            "hash": "0cff9d7d8cb4c32588e5776e165d9136acb79eae"
          }
        ]
      }
    ]
  },
  {
    "version": "0.3.0",
    "date": "2026-08-07",
    "sections": [
      {
        "title": "🎉 Features",
        "audience": "all",
        "items": [
          {
            "description": "Rename the harness id to rovo and qualify the frontmatter key (#1199)",
            "body": "Renames the internal ID for the Rovo Dev harness from `rovodev` to `rovo`. `--harness rovo` replaces `--harness rovodev` in every command.\n\nSeparately, renames the `harnesses` frontmatter key (which narrows a skill to particular harnesses) to `supported-harnesses:` to avoid confusion.",
            "breaking": true,
            "hash": "cb47091488d0c2e308aeffa12b58050004bc5bf1"
          }
        ]
      }
    ]
  },
  {
    "version": "0.2.1",
    "date": "2026-08-05",
    "sections": [
      {
        "title": "🧪 Tests",
        "audience": "dev",
        "items": [
          {
            "description": "Name every test file's isolation tier (#1185)",
            "body": "Every test file now embeds the name of its tier (unit or tool), each of which can be run separately (`nmr test:unit` or `nmr test:tool`). Git isolation settings that had previously been removed from the root-level test configurations are now restored.",
            "hash": "b53e4acfb3cff7eeaee6b1a6cbc6da01c01b8ac2"
          }
        ]
      }
    ]
  },
  {
    "version": "0.2.0",
    "date": "2026-08-04",
    "sections": [
      {
        "title": "🎉 Features",
        "audience": "all",
        "items": [
          {
            "description": "Bound fold memory and rescan cost with a retention window (#1057)",
            "body": "Defines a retention window so that a Fleet server run against a long-lived events root no longer grows without bound. A branch that stays idle past the retention window (three days by default) is now dropped from the fleet, so the server's memory footprint and per-rescan filesystem cost only track currently-active work. A dropped branch reappears if it becomes active again.",
            "hash": "7e19098705c37c7b7ba8c2b30822001720f0bead"
          },
          {
            "description": "Add the read-only git adapter for worktree and base-branch ground truth (#1059)",
            "body": "Fleet's lane view now reflects each worktree's real git state (the checked-out branch, the number of uncommitted files, and how far ahead or behind the base branch it sits), refreshed on a configurable interval. A lane whose worktree has been deleted now closes on its own, instead of lingering indefinitely.",
            "hash": "4169d7c3e1e5797c2214e8e99d8bd4f6f5bec23d"
          },
          {
            "description": "Make codeassembly and kb CLI tools publishable (#1164)",
            "body": "The `codeassembly` CLI now installs from npm, and its `install` and `sync` commands deploy the rulebooks, skills, and subagents it ships into any consuming project. The knowledge-base library `@williamthorsen/kb` and the session-lifecycle event package `codeassembly-lifecycle` are published alongside it.",
            "hash": "e8f7a6ddd4eeb7f0cfda594d5cee120cf1bc369a"
          }
        ]
      },
      {
        "title": "🏗️ Internal features",
        "audience": "dev",
        "items": [
          {
            "description": "Add lifecycle workspace with the canonical envelope, vocabulary & lane fold (#1049)",
            "body": "Introduces `@codeassembly/lifecycle`, a shared workspace package that houses the canonical lifecycle-event envelope, the event vocabulary, and the fold that turns a lane's events into session and lane state. The package is dependency-free and browser-bundle-safe.",
            "hash": "0dd4d156b8725aa7bd22d9887413e9cfd587c96e"
          }
        ]
      },
      {
        "title": "♻️ Refactoring",
        "audience": "dev",
        "items": [
          {
            "description": "Rename packages to publishable names (#1157)",
            "body": "Renames CodeAssembly packages in preparation for their initial publication. The package responsible for deploying and syncing agent guidance is now called `codeassembly`.",
            "hash": "f508636e70f89ce8e3733dac59b3b3ea432a9860"
          }
        ]
      },
      {
        "title": "⚙️ Tooling",
        "audience": "dev",
        "items": [
          {
            "description": "Move compilation out of the install lifecycle into a bootstrap step (#1102)",
            "body": "Fixes an issue where installation of dependencies failed intermittently. Reaching a usable tree afterward now takes one command, `pnpm run bootstrap`, and every workflow that needs a built tree runs it. A command-line tool invoked before bootstrapping now points to a command that exists.",
            "hash": "7c195d2f9757492d8606cd0659ac1c9b94aadac9"
          },
          {
            "description": "Migrate Vitest to nmr's centralized model (#1154)",
            "body": "Changes Vitest configuration so that test suites are selected by project (\"unit\", \"integration\", and \"app\"), eliminating the need for category-specific configuration files. Every package keeps a single Vitest config file, which composes the repo's shared settings rather than carrying its own copy. The nmr fmt command now formats shell scripts as well, and the corresponding package-file scripts have been removed as redundant.",
            "hash": "cbcd30a4755a37696236c0deef02e8b8ed2f8873"
          },
          {
            "description": "Run every test in the default gate, classified by what it reaches (#1155)",
            "body": "Upgrades `nmr` to 0.24, which changes Vitest configuration so that test suites are selected by a tier (\"unit\", \"tool\", \"localhost\", and \"remote\") corresponding to the services they use. `nmr test:unit` and `nmr test:tool` each run one of these; `nmr test:all` runs every suite. All tests are covered by the default run. `nmr test:integration` no longer exists, and no tests carry the `.int.` infix. The upgraded `nmr` includes a caching feature that skips checks that already succeeded against an identical working tree.",
            "hash": "2efd05b0be54e7cc59e98b53d2f5d145610420be"
          }
        ]
      }
    ]
  }
]
